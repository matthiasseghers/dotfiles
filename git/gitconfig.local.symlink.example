# ~/.gitconfig.local — primary git identity
# Generated by script/bootstrap via sed. NEVER commit this file.
# For folder-scoped identities, use bin/git-add-identity (writes to ~/.gitconfig.identities)

[user]
	name = AUTHORNAME
	email = AUTHOREMAIL