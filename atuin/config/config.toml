# Atuin configuration
# https://docs.atuin.sh/configuration/config/

# Execute selected history item immediately on Enter; Tab inserts for editing.
enter_accept = true

[sync]
# Enable sync v2.
records = true
